# ContentRus.TenantManagement

## Database
The database (mySQL) is created using docker.
```bash
docker compose up -d
```

## API
After the database is up, you can run the API using the following command:
```bash
dotnet run
```