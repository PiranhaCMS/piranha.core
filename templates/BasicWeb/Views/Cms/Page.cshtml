@model BasicWeb.Models.StandardPage
@{
    ViewBag.Title = Model.Title;
}

<div class="main">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-sm-8 page-body">
                <h1>@Model.Title</h1>
                @Html.Raw(Model.Body)
            </div>
        </div>
    </div>
</div>
